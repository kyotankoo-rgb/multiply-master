{
  "name": "multiply-master",
  "version": "1.0.0",
  "description": "Multiply Master - Math Game for Kids",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync && cd android && ./gradlew assembleDebug",
    "cap:release": "npx cap sync && cd android && ./gradlew assembleRelease"
  },
  "keywords": [
    "math",
    "game",
    "education",
    "multiplication"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor-community/admob": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/haptics": "^5.0.0",
    "@capacitor/share": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "typescript": "^5.9.3"
  }
}
